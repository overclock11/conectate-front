import { IntegracionPipe } from './integracion.pipe';

describe('IntegracionPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegracionPipe();
    expect(pipe).toBeTruthy();
  });
});
